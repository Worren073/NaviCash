"""core — Utilidades transversales del dominio de NaviCash.

Contiene:
- ``currency``: catálogo de monedas y ayuda de conversión (solo Decimal, nunca float).
- ``models``:  modelos base reutilizables (UUID pk, marcas de tiempo).
- ``pagination``: paginación por defecto de la API.
- ``permissions``: permisos personalizados de DRF.
- ``exceptions``: formato de errores uniforme para la API.
"""